# The tools the gate runs that no stack owns, pinned. One version per tool,
# here and nowhere else: `bun run check` resolves each binary through
# `mise which`, and the ci workflow installs from this file.
#
# A bare name comes from mise's registry, which routes it to a release artifact
# through the aqua backend. mise.lock holds the artifact each version resolved
# to, per platform in lockfile_platforms, and the gate reads that file against
# the exact artifact urls, api prefixes and provenance scripts/tools.ts expects
# before anything installs from it. `mise lock` rewrites it after any edit to [tools].
#
# This file holds [tools], [tool_config] and [settings] alone, and scripts/tools.ts holds
# the last two as well, compared whole: another table, or a setting changed here and not
# there, is a finding.
#
# taplo's release predates GitHub's asset digests and its registry entry names
# no checksum file, so no mise command writes a checksum for it. Its three
# checksum lines are the sha256 of the artifact at each recorded url, computed
# by hand from a download. A relock at the same version keeps them and a bump
# drops them, so a taplo bump recomputes all three. A bump to a release that
# carries a digest removes the exception.

[tools]
actionlint = "1.7.12"
shellcheck = "0.11.0"
taplo = "0.10.0"
zizmor = "1.30.1"

[tool_config]
# The locked mode no environment variable reaches. mise refuses to install a
# version mise.lock does not record, so an edit to [tools] with no relock is
# refused rather than fetched. No settings command reports this key.
locked = true

[settings]
# The same refusal at the settings level, which is what `mise which` answers
# for. MISE_LOCKED=false or a locked_scopes list without project turns it off,
# and neither reaches the [tool_config] line above.
locked = true
# mise.lock is written as well as read, so a bump records what it resolved to.
lockfile = true
# The platforms mise.lock pins, which are the platforms the ci matrix runs.
lockfile_platforms = ["linux-x64", "macos-arm64", "windows-x64"]
# mise falls back to a lockfile entry's url_api when a HEAD on its url fails,
# and that address names an asset by a bare id nothing offline ties to a
# version. This rule sends every such address to a host that does not resolve,
# so the fallback fails rather than installing another release. The gate
# asserts the rule and pins the same map through MISE_URL_REPLACEMENTS on its
# install, because a config file mise reads later can replace this one.
url_replacements = { "regex:^https://api\\.github\\.com/repos/[^/]+/[^/]+/releases/assets/.*$" = "https://url-api-refused.invalid/" }
# Every install re-verifies an attested artifact against its attestation. The
# expectation comes from the lockfile's own entry, so this arms the downgrade
# refusal for an entry that claims provenance and replaces none of the
# lockfile assertions in scripts/tools.ts.
locked_verify_provenance = true
# An attestation query that cannot complete is an install error rather than a
# skipped check.
provenance_api_failures_fatal = true
# The attestation switches, written out because a default is a thing that can
# change under the repository.
github_attestations = true

[settings.aqua]
github_attestations = true
